<%
  color ||= "secondary"
  icon ||= ""
  classes ||= ""
%>
<%= tag.span class: "badge bg-#{color} #{classes}" do %>
  <%= tag.i "", class: icon if icon.present? %>
  <%= capture(&block) %>
<% end %>