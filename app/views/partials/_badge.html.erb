<%
  color ||= "secondary"
  icon ||= ""
  classes ||= ""
%>
<%= tag.span class: "badge bg-#{color} #{classes} mb-1 mb-sm-0" do %>
  <%= tag.i "", class: icon if icon.present? %>
  <%= capture(&block) %>
<% end %>