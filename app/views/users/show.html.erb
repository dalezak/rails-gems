<% title ["Devs", @user] %>
<% description @user.title || @user.description %>

<%= turbo_frame_tag dom_id(@user, :details) do %>
  <%= render "/users/details", user: @user %>
<% end %>

<%= turbo_frame_tag "gems", src: user_gems_path(@user), loading: :lazy %>